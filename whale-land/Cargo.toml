[package]
name = "whale-land"
version = "0.1.0"
edition = "2024"

[dependencies]
async-trait = { version = "0.1" }
libc = { version = "0.2" }
tokio = { version = "1.52", features = ["net", "sync"] }
tracing = { version = "0.1" }
